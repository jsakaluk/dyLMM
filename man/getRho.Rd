% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getRho.R
\name{getRho}
\alias{getRho}
\title{Extract Compound Symmetry Correlation (Rho) from a gls Model}
\usage{
getRho(fit, ci = NULL)
}
\arguments{
\item{fit}{A fitted \code{gls} object from \code{\link{fitAPIMindist}},
\code{\link{fitAPIMdist}}, or \code{\link{fitICC}}.}

\item{ci}{Numeric or \code{NULL}. If numeric (e.g., 0.95), compute a confidence
interval using \code{\link[nlme:intervals]{nlme::intervals()}}. If \code{NULL}, no CI is computed.}
}
\value{
A numeric vector with \code{rho} and optionally \code{CI_low} and \code{CI_high}.
}
\description{
Extracts the compound symmetry correlation (rho) from a fitted \code{gls} model
with \code{corCompSymm}. For APIM fits (e.g., from \code{\link[=fitAPIMindist]{fitAPIMindist()}} or
\code{\link[=fitAPIMdist]{fitAPIMdist()}}), rho is the partial intraclass correlation--the residual
correlation between dyad members controlling for predictors. For \code{\link[=fitICC]{fitICC()}}
(unconditional model), rho equals the ICC.
}
\examples{
\dontrun{
library(dySEM)
dvn <- scrapeVarCross(dat = commitmentQ, x_order = "spi", x_stem = "sat.g",
  x_delim1 = ".", x_delim2 = "_", distinguish_1 = "1", distinguish_2 = "2",
  y_order = "spi", y_stem = "com", y_delim1 = ".", y_delim2 = "_",
  verbose = FALSE)
dat_comp <- build_composites(dat = commitmentQ, dvn = dvn)
dat_long <- wide_to_long(dat_comp)
fit <- fitAPIMindist(data = dat_long, y = "y", x = "x")
getRho(fit)
getRho(fit, ci = 0.95)
}
}
\seealso{
\code{\link[=getICC]{getICC()}} for the ICC from the unconditional model; \code{\link[=fitAPIMindist]{fitAPIMindist()}},
\code{\link[=fitAPIMdist]{fitAPIMdist()}} for APIM fits.
}
