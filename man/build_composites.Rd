% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_composites.R
\name{build_composites}
\alias{build_composites}
\title{Build Composite Scores from Indicator Variables}
\usage{
build_composites(
  dat,
  dvn = NULL,
  x_vars = NULL,
  y_vars = NULL,
  aggregate = c("mean", "sum"),
  x_name = "x",
  y_name = "y",
  dyad_id = NULL
)
}
\arguments{
\item{dat}{Data frame with indicator variables (wide/dyad format).}

\item{dvn}{Optional. List from \code{dySEM::scrapeVarCross()} with \code{p1xvarnames},
\code{p2xvarnames}, and optionally \code{p1yvarnames}, \code{p2yvarnames}. When \code{dvn} has
both X and Y (from scrapeVarCross with y_stem), both composites are built.}

\item{x_vars}{Optional. List with elements \code{p1} and \code{p2}, each a character
vector of variable names. Use when not providing \code{dvn}.}

\item{y_vars}{Optional. List with elements \code{p1} and \code{p2}, each a character
vector. Use when not providing \code{dvn} and Y composites are needed.}

\item{aggregate}{Either \code{"mean"} (default) or \code{"sum"} for computing composites.}

\item{x_name}{Character stem for X composite column names (default \code{"x"}).
Produces \verb{\{x_name\}_p1}, \verb{\{x_name\}_p2}.}

\item{y_name}{Character stem for Y composite column names (default \code{"y"}).}

\item{dyad_id}{Character. Name of column in \code{dat} that identifies dyads. If
\code{NULL}, a \code{dyad_id} column is created from row indices (1:nrow).}
}
\value{
Data frame with original columns plus \verb{\{x_name\}_p1}, \verb{\{x_name\}_p2},
and optionally \verb{\{y_name\}_p1}, \verb{\{y_name\}_p2}, and \code{dyad_id} (if not already present).
}
\description{
Calculate sum-scores or mean-scores for X and Y from indicator variables.
Accepts a \code{dvn} object from \code{dySEM::scrapeVarCross()} or explicit variable lists.
}
\examples{
\dontrun{
library(dySEM)
dvn <- scrapeVarCross(
  dat = commitmentQ, x_order = "spi", x_stem = "sat.g",
  x_delim1 = ".", x_delim2 = "_", distinguish_1 = "1", distinguish_2 = "2",
  y_order = "spi", y_stem = "com", y_delim1 = ".", y_delim2 = "_",
  verbose = FALSE
)
dat_comp <- build_composites(dat = commitmentQ, dvn = dvn)
}
}
