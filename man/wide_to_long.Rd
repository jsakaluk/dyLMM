% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wide_to_long.R
\name{wide_to_long}
\alias{wide_to_long}
\title{Reshape Wide Dyad Data to Long Format}
\usage{
wide_to_long(
  dat,
  x_p1 = "x_p1",
  x_p2 = "x_p2",
  y_p1 = "y_p1",
  y_p2 = "y_p2",
  dyad_id = "dyad_id",
  partner_col = "partner",
  x_col = "x",
  x_partner_col = "x_partner",
  y_col = "y",
  distinguish = NULL,
  partner_labels = NULL
)
}
\arguments{
\item{dat}{Data frame in wide format. Must have columns for actor and partner
values of X and Y, or be passed after \code{build_composites()}.}

\item{x_p1, x_p2}{Column names for partner 1 and 2's X (predictor). Default
\code{"x_p1"}, \code{"x_p2"}.}

\item{y_p1, y_p2}{Column names for partner 1 and 2's Y (outcome). Default
\code{"y_p1"}, \code{"y_p2"}.}

\item{dyad_id}{Column name identifying dyads. Default \code{"dyad_id"}.}

\item{partner_col}{Name for the new column indicating partner (1 vs 2).}

\item{x_col, x_partner_col}{Names for actor's X and partner's X in long format.}

\item{y_col}{Name for outcome in long format.}

\item{distinguish}{Optional. Column name(s) in \code{dat} that distinguish partners
(e.g., gender). If provided, these are included in the long output.}

\item{partner_labels}{Optional. Length-2 character or numeric vector giving
labels for partner 1 and 2 (e.g., \code{c("f", "m")} or \code{c("Wife", "Husband")}).
When provided, a new column \code{distinguish} is created from these labels.}
}
\value{
A tibble in long format with columns: \code{dyad_id}, \code{partner}
(1/2), \code{x} (actor's X), \code{x_partner} (partner's X), \code{y} (outcome), and
optionally \code{distinguish} if provided.
}
\description{
Transform wide-format dyadic data (one row per dyad) to long format (two rows
per dyad) suitable for LMM fitting. Works with pre-computed composites or
with columns named \code{x_p1}, \code{x_p2}, \code{y_p1}, \code{y_p2}.
}
\examples{
\dontrun{
library(dySEM)
dvn <- scrapeVarCross(
  dat = commitmentQ, x_order = "spi", x_stem = "sat.g",
  x_delim1 = ".", x_delim2 = "_", distinguish_1 = "1", distinguish_2 = "2",
  y_order = "spi", y_stem = "com", y_delim1 = ".", y_delim2 = "_",
  verbose = FALSE
)
dat_comp <- build_composites(dat = commitmentQ, dvn = dvn)
dat_long <- wide_to_long(dat_comp)
}
}
