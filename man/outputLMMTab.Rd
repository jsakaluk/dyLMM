% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/outputLMMTab.R
\name{outputLMMTab}
\alias{outputLMMTab}
\title{Export Parameter Table from Fitted LMM}
\usage{
outputLMMTab(fit, gtTab = FALSE, writeTo = NULL, fileName = NULL)
}
\arguments{
\item{fit}{A fitted \code{lmerMod} object from \code{lme4::lmer} or
\code{lmerTest::lmer}.}

\item{gtTab}{Logical. If \code{TRUE}, return a \code{\link[gt:gt]{gt::gt()}} object. If \code{FALSE}
(default), return a \code{\link[tibble:tibble]{tibble::tibble()}}.}

\item{writeTo}{Optional. Directory path to save the table as \code{.rtf}. Requires
\code{gtTab = TRUE}.}

\item{fileName}{Optional. Base filename for the \code{.rtf} (without extension).}
}
\value{
A tibble (if \code{gtTab = FALSE}) or gt object (if \code{gtTab = TRUE}) with
columns: Parameter, Estimate, SE, Statistic (t), p-value.
}
\description{
Creates a table of fixed-effects parameter estimates and statistical tests
from a fitted lme4/lmerTest model. Returns a tibble or a \code{gt} object for
publication. P-values use Satterthwaite degrees of freedom when the model
was fit with \code{lmerTest::lmer()} (via \code{use_lmerTest = TRUE} in the
fitting functions); otherwise a z-approximation is used.
}
\details{
When \code{gtTab = TRUE} and \code{writeTo} is specified, the table is saved
as an \code{.rtf} file. A confirmation message is printed on success.
}
\examples{
\dontrun{
library(dySEM)
dvn <- scrapeVarCross(dat = commitmentQ, x_order = "spi", x_stem = "sat.g",
  x_delim1 = ".", x_delim2 = "_", distinguish_1 = "1", distinguish_2 = "2",
  y_order = "spi", y_stem = "com", y_delim1 = ".", y_delim2 = "_",
  verbose = FALSE)
dat_comp <- build_composites(dat = commitmentQ, dvn = dvn)
dat_long <- wide_to_long(dat_comp)
fit <- fitAPIMindist(data = dat_long, y = "y", x = "x")
outputLMMTab(fit)
outputLMMTab(fit, gtTab = TRUE)
}
}
